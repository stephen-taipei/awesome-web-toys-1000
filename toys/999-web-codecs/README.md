# Web Codecs Demo

Demonstrates the use of the `VideoEncoder` and `VideoDecoder` APIs to encode frames from a source canvas and decode them to a destination canvas in real-time.

## Note
Requires a browser that supports the WebCodecs API (Chrome 86+).
